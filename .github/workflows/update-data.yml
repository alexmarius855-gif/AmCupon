name: Actualizare date zilnica

on:
  schedule:
    - cron: '0 4 * * *'  # 6:00 ora Romaniei (UTC+2)
  workflow_dispatch:      # pornire manuala din GitHub

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Instaleaza dependinte Python
        run: pip install pandas openpyxl requests

      - name: Ruleaza procesare date 2Performant
        run: |
          cd scripts
          python process_data.py

      - name: Ruleaza procesare date Profitshare
        env:
          PROFITSHARE_USER: ${{ secrets.PROFITSHARE_USER }}
          PROFITSHARE_KEY: ${{ secrets.PROFITSHARE_KEY }}
        run: |
          cd scripts
          python process_profitshare.py

      - name: Merge date din toate platformele
        run: |
          cd scripts
          python merge_platforms.py

      - name: Genereaza articole blog
        run: |
          cd scripts
          python generate_blog.py

      - name: Commit si push daca s-au schimbat datele
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add frontend/public/output.json data/output.json frontend/public/blog-posts.json
          git diff --staged --quiet && echo "Nicio schimbare." || (
            git commit -m "auto: actualizare date si blog $(date +'%Y-%m-%d')"
            git push
          )
